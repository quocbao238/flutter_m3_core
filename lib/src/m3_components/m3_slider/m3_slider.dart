import 'package:flutter/material.dart';

class M3Slider extends StatelessWidget {
  const M3Slider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
